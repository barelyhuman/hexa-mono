{
  "name": "@tillwhen/adaptor",
  "description": "adator that defines the behaviours of each datasource",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./dist/index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsup src/index.ts --dts --format=cjs",
    "dev": "tsup src/index.ts --watch --dts --format=cjs"
  },
  "dependencies": {
    "@tillwhen/postgres": "1.0.0"
  }
}
