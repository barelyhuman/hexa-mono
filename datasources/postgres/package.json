{
  "name": "@tillwhen/postgres",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./dist/index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsup src/index.ts --dts --format=cjs",
    "dev": "tsup src/index.ts --watch --dts --format=cjs"
  },
  "dependencies": {
    "knex": "^2.3.0",
    "sqlite3": "^5.1.3"
  }
}
